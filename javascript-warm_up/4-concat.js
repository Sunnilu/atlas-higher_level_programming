#!/usr/bin/node
// script that prints two arguments passed to it.

const arg1 = process.argv[2];
const arg2 = process.argv[3];

// checking if at least two arguments are passed
console.log(arg1 + " is " + arg2); // Corrected line
