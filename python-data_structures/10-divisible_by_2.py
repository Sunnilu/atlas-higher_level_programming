#!/usr/bin/python3
def divisible_by_2(my_list=[]):
    results = []
    for num in my_list:
        if num % == 0:
            result.append(True)
        else:
            result.append(False)
    return result
